node {
   def mvnHome
   def gitRep = 'https://github.com/JeezLF/ITIIP15-DevOps-MurlopsTeam.git'
   stage('Clone') { // for display purposes		
		echo 'git clone'
		git $gitRep
	  
   }
   stage('Build') {
		echo 'build'
		sh 'gradlew build'
   }
   stage('Results') {
		echo 'result'
   }
}
