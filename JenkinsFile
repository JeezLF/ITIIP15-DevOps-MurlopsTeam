node {
   stage('Clone') { // for display purposes		
		git 'https://github.com/JeezLF/ITIIP15-DevOps-MurlopsTeam.git'
	  
   }
   stage('Build') {
		sh 'gradlew build'
   }
   stage('Results') {
   }
}
